{% block menu %}
    <ul class="nav navbar-nav">
        <li><a href="{{ path('pingtest') }}">Pingtest</a></li>
        <li><a href="{{ path('maptest') }}">Maptest</a></li>
        {% if is_granted('ROLE_ADMIN') %}
            <li><a href="{{ path('pingtest') }}">SSH</a></li>
            <li><a href="{{ path('pingtest') }}">Users</a></li>
        {% endif %}
        <li><a href="{{ path('about') }}">About</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
        <!-- Authentication Links -->
        {% if app.user %}
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                    <i class="glyphicon glyphicon-user"></i>
                    {{ app.user.username }} <span class="caret"></span>
                </a>

                <ul class="dropdown-menu" role="menu">
                    <li><a href="{{ path('pingtest', {'id': app.user.id}) }}">Change Password</a></li>
                    <li><a href="{{ path('logout') }}">Logout</a></li>
                </ul>
            </li>
        {% else %}
            <li><a href="{{ path('login') }}">Login</a></li>
        {% endif %}
    </ul>
{% endblock %}
